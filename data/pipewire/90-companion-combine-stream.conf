# Generated by Companion. Safe to delete; edits here may be overwritten.
# Engine for "Play on multiple" (Share). Docs: module-combine-stream.
context.modules = [
    {
        name = libpipewire-module-combine-stream
        args = {
            combine.mode = sink
            node.name    = "companion_combine"
            node.description = "Companion - Play on multiple"
            combine.latency-compensate = true
            combine.props = {
                audio.position = [ FL FR ]
            }
            stream.props = { }
            stream.rules = [
            {
                matches = [ { node.name = "bluez_output.AA_BB_CC_DD_EE_01" } ]
                actions = {
                    create-stream = {
                        combine.audio.position = [ FL FR ]
                        audio.position         = [ FL FR ]
                        node.latency-offset-nsec = 0
                    }
                }
            }
            {
                matches = [ { node.name = "bluez_output.AA_BB_CC_DD_EE_02" } ]
                actions = {
                    create-stream = {
                        combine.audio.position = [ FL FR ]
                        audio.position         = [ FL FR ]
                        node.latency-offset-nsec = 40000000
                    }
                }
            }
            ]
        }
    }
]
